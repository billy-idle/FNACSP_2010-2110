package net.ddns.starla.fnacsp.template.entities;

/**
 * Same constructor as Entity but not the same type.
 * Only for testing purposes
 * (Java does not allow Duck-Typing)
 */
class EntityDuTy {
    public EntityDuTy(Object value) {
    }
}