package net.ddns.starla.fnacsp.template.algorithms;

import net.ddns.starla.fnacsp.template.entities.Entity;

/**
 * Same constructor as Algorithm but not the same type.
 * Only for testing purposes
 * (Java does not allow Duck-Typing)
 */
class AlgorithmDuTy {
    public AlgorithmDuTy(Entity time, Entity longitude, Entity latitude, Entity pressure, Entity temperature) {
    }
}